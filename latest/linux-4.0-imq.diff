diff --git a/drivers/net/imq.c b/drivers/net/imq.c
index f4aff23..f9f3285 100644
--- a/drivers/net/imq.c
+++ b/drivers/net/imq.c
@@ -656,6 +656,9 @@ static int __imq_nf_queue(struct nf_queue_entry *entry, struct net_device *dev)
 					}
 				}
 			}
+		} else {
+			/* No ready skb, then schedule it */
+			__netif_schedule(q);
 		}
 		rcu_read_unlock_bh();
 		retval = 0;
